#include "messaging.hpp"

namespace ucp {

  void messaging::send( const string& msg ) {
  }
  void messaging::send( const char* buffer, unsigned int length ) {
  }
  void messaging::receive( string& buffer ) {
  }


}
